import { DocumentActionDescription, DocumentActionProps, DocumentActionComponent, DocumentBadgeComponent } from "sanity/desk";
declare global {
    interface WindowEventMap {
        [ScheduleEvents.create]: CustomEvent<ScheduleCreateEvent>;
        [ScheduleEvents.delete]: CustomEvent<ScheduleDeleteEvent>;
        [ScheduleEvents.deleteMultiple]: CustomEvent<ScheduleDeleteMultipleEvent>;
        [ScheduleEvents.publish]: CustomEvent<SchedulePublishEvent>;
        [ScheduleEvents.update]: CustomEvent<ScheduleUpdateEvent>;
    }
}
export const ScheduleAction: (props: DocumentActionProps) => DocumentActionDescription;
type Action = DocumentActionComponent;
export function resolveDocumentActions(existingActions: Action[]): Action[];
export const ScheduledBadge: DocumentBadgeComponent;
export function resolveDocumentBadges(existingBadges: DocumentBadgeComponent[]): DocumentBadgeComponent[];
export const scheduledPublishing: import("sanity").Plugin<void>;

//# sourceMappingURL=index.d.ts.map
